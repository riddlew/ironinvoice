app:
  cors:
    allowed-origins: "*"
  security:
    jwt:
      access-ttl-minutes: 30
      issuer: "ironinvoice-test"
      refresh-ttl-days: 30
      secret: "xtdTheh5X2yUpdAEr6RDghyEKNpExB2j6BFQPDbeBlm"
  storage:
    max-bytes: 1048576
    sample-limit: 100

spring:
  application:
    name: "ironinvoice-api-test"
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
